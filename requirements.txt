# Top-level dependencies (what eurusdt_pro.py actually imports)
flask==3.1.3
websocket-client==1.9.0
requests==2.34.2

# Production WSGI server (optional - only needed if you run via
# `gunicorn eurusdt_pro:app` instead of `python eurusdt_pro.py`.
# Linux/Mac only - does not work on Windows or Pydroid/Android.
# ALWAYS run with --workers 1 for this app: it keeps live state
# (order book, Kalman filter) in memory and opens its own Binance
# WebSocket connection, so extra workers just duplicate everything.)
gunicorn==26.0.0

# Transitive dependencies (pulled in automatically by the above,
# pinned here for a fully reproducible install)
blinker==1.9.0
certifi==2026.6.17
charset-normalizer==3.4.7
click==8.4.1
idna==3.18
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
packaging==26.2
urllib3==2.7.0
Werkzeug==3.1.8
